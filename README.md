# Gui's Photos

Photography portfolio and GitHub Pages site for Gui Roque.

This repository contains the source for the public portfolio at `guis.photos`. It is a static site built with HTML, CSS, and JavaScript. The gallery is generated from the image files in the repository so new work can be added without editing gallery markup by hand.

## Copyright

Copyright (c) 2026 Gui Roque. All rights reserved.

All photographs, images, written content, and visual assets in this repository are owned by Gui Roque unless otherwise stated. They may not be copied, reused, redistributed, modified, trained on, or used commercially without written permission.

The source code is included publicly so the GitHub Pages site can be hosted and reviewed. No open source license is currently granted for this repository.

For permission requests, contact: `eoqrupics@gmail.com`.

## Adding Photos

1. Add image files to `assets/images/`.
2. Commit and push to `main`.
3. GitHub Pages runs `npm run generate:gallery` during deployment.
4. New images appear at the top of the gallery automatically.

For a local preview after adding images:

```bash
npm run generate:gallery
```

Do not edit `data/gallery.js` manually. It is generated by `scripts/build-gallery-data.js`.

## Image Metadata

Optional metadata lives in `data/gallery-meta.json`.

Supported fields:

- `alt` - accessible image description.
- `cameraSettings` - camera and lens information.
- `imageResolution` - shot settings and resolution.

The work detail page only shows camera information when `cameraSettings` or `imageResolution` exists for that image.

If `assets/images/about-portrait.jpg` is added later, the generator skips it so it can be used by the about page instead of the gallery.

## Project Structure

```text
.
├── assets/
│   ├── icons/
│   └── images/
├── components/
│   ├── faq-list.js
│   ├── site-header.js
│   ├── work-detail.js
│   └── work-grid.js
├── data/
│   ├── faq.js
│   ├── gallery-meta.json
│   └── gallery.js
├── scripts/
│   └── build-gallery-data.js
├── styles/
│   └── global.css
├── views/
│   ├── about.html
│   ├── faq.html
│   └── work.html
├── index.html
└── CNAME
```

## Local Use

Open `index.html` in a browser.

The site does not require a local dev server because it does not use runtime `fetch()` calls.

## Deployment

Deployment is handled by `.github/workflows/pages.yml`.

On every push to `main`, the workflow:

1. Checks out the repository.
2. Runs `npm run generate:gallery`.
3. Uploads the static site files to GitHub Pages.
